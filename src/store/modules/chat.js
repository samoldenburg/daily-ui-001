import * as types from '../mutation-types'

// initial state
const state = {
    error: false,
    loading: true,
    messages: []
}

// mutations
const mutations = {
}

export default {
    state,
    mutations
}
